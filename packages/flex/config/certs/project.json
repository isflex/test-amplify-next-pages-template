{
  "name": "@flexiness/certs",
  "prefix": "@flexiness",
  "sourceRoot": "packages/flex/config/certs",
  "projectType": "library",
  "targets": {
    "compile:tsup": {
      "executor": "nx:run-script",
      "inputs": [
        "{projectRoot}/package.json",
        "{projectRoot}/tsup.config.ts",
        "{projectRoot}/src"
      ],
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "script": "compile:tsup"
      }
    }
  },
  "implicitDependencies": [],
  "tags": ["scope:flexiness-staging"]
}
